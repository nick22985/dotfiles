##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# discord & slack always on 9 and 10
windowrule = workspace 9 silent,match:class (discord)$
windowrule = workspace 10 silent,match:class (slack)$, no_initial_focus on

# flameshot setup
# noanim isn't necessary but animations with these rules might look bad. use at your own discretion.
windowrule = match:class ^(flameshot)$, no_anim 1
windowrule = match:class ^(flameshot)$, float 1
windowrule = match:class ^(flameshot)$, move 0 0
windowrule = match:class ^(flameshot)$, pin 1
windowrule = match:class ^(flameshot)$, no_initial_focus on
# set this to your leftmost monitor id, otherwise you have to move your cursor to the leftmost monitor
# before executing flameshot
# windowrule = monitor 0, class:^(flameshot)$
exec-once = flatpak run org.flameshot.Flamehsot;

# 1Password quick access popup centering
windowrule = center 1, match:class ^(1Password)$, match:title ^(Quick Access — 1Password)$
windowrule = stay_focused on, match:class ^(1Password)$, match:title ^(Quick Access — 1Password)$
windowrule= pin on, match:class ^(1Password)$, match:title ^(Quick Access — 1Password)$
# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[t1], gapsout:0, gapsin:0
# workspace = w[tg1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[t1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[t1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tg1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tg1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]
windowrule = opacity 0.0 override, match:class ^(xwaylandvideobridge)$
windowrule = no_anim 1, match:class ^(xwaylandvideobridge)$
windowrule = no_initial_focus on, match:class ^(xwaylandvideobridge)$
windowrule = no_focus on, match:class ^(xwaylandvideobridge)$
windowrule = max_size 1 1, match:class ^(xwaylandvideobridge)$
windowrule = no_blur on, match:class ^(xwaylandvideobridge)$
# Ignore maximize requests from apps. You'll probably like this.
windowrule= suppress_event maximize, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus on,match:class ^$, match:title ^$, match:xwayland 1,match:float 1,fullscreen 0, pin 0
